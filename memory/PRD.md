# LastMile OS - Product Requirements Document (PRD)

## Original Problem Statement
Build "LastMile OS MVP" for managing last-mile delivery operations for ME (Mensajeria y Estrategias). Includes authentication, dashboard polling, CSV/XLSX Cosmo data layout uploads, journey execution tracking (Start/Incidents/Close), dynamic assignment, AI-powered evidence scoring with discrepancy detection, real-time dashboard, advanced reporting, quality criteria configurations, admin AI consumption tracking, webhook integrations, and API documentation.

## Tech Stack
- **Backend**: FastAPI, Motor (MongoDB), Python 3.11
- **Frontend**: React 18, TailwindCSS, Shadcn UI, Recharts, DOMPurify
- **Database**: MongoDB
- **AI**: Claude Sonnet 4.5 via Emergent LLM Key
- **Maps**: Leaflet + CartoDB Positron tiles
- **Webhooks**: httpx async dispatch with HMAC-SHA256 signatures

## User Personas
- **Agent** (`agente@me.mx`): Field delivery agent. Views assigned routes and packages.
- **Coordinator** (`yael@me.mx`): Manages operations, reviews quality, configures settings, admin access. Can edit SLA and Pulse config.
- **Developer** (`dev@me.mx`): Full access, configures system, tests API, admin module, can upload layouts.
- **Executive** (`karina@me.mx`): High-level dashboards, reports, admin read-only.
- **Proveedor** (`proveedor@me.mx`): Read-only restricted view.

## Core Requirements (Status)
- [x] JWT role-based authentication (Agent, Coordinator, Developer, Executive)
- [x] httpOnly cookie auth (secure, SameSite=lax) with Bearer header backward compat
- [x] 2-step Cosmo layout upload (history-orders CSV + route-summary XLSX)
- [x] Journey management (Start, Incidents, Close) with image uploads
- [x] Kosmo tracking sync (automatic + manual) with optimized performance
- [x] AI-powered evidence scoring (Claude Sonnet) with structured JSON output
- [x] Real-time WebSocket dashboard with geographic heatmaps
- [x] Advanced reporting with 6 tabs (SLA, Attempts, Quality, etc.)
- [x] Lumi AI Chatbot
- [x] Quality Criteria V2 (5-tab settings)
- [x] Quality Tab V2 in Journey Detail
- [x] Supervised training (correct/incorrect + natural language notes + score override)
- [x] Admin IA Module (Token Consumption, Routes Report, Cost Config)
- [x] API Documentation with sandbox, Webhooks reference, and code examples
- [x] Database indexes + parallel query optimization
- [x] Webhook Integrations (Plug & Play) - CRUD, test, HMAC, delivery log
- [x] Backend refactoring - all routes in /routes/ directory
- [x] Code quality: XSS sanitization (DOMPurify), Python complexity reduction, table-driven middleware
- [x] Resilient data loading: Promise.allSettled in all page fetchers
- [x] Test security: credentials via env vars (conftest.py)
- [x] Dynamic SLA Configuration per Provider
- [x] Pulse: Real-time feasibility monitor
- [x] Discrepancy Detection with Confidence Engine
- [x] AI Evaluation UX: Severity badges (critical/warning) in GuiasTab
- [x] AI Evaluation UX: Quick Review Modal (decoupled from table row)
- [x] AI Evaluation UX: Background execution with polling progress banner

## Architecture
```
/app/backend/
  server.py (210 lines - app setup only)
  dependencies.py (refactored: extracted address helpers, httpOnly cookie config)
  middleware.py (refactored: table-driven patterns)
  evidence_scoring.py (refactored: 5 extracted helpers + status tracking)
  kosmo_sync.py (optimized: 10x concurrency, connection pooling, 429 handling)
  token_logger.py, cp_coordinates.py, ws_manager.py

  routes/ (14 modules via __init__.py)
    auth_routes.py (httpOnly cookies on login/logout)
    user_routes.py, journey_routes.py
    upload_routes.py, dashboard_routes.py, analytics_routes.py
    admin_routes.py, admin_module_routes.py
    quality_criteria_routes.py, quality_tab_routes.py
    webhook_routes.py, system_routes.py
    lumi_routes.py, kosmo_routes.py

  tests/
    conftest.py (shared fixtures, env-based credentials)
    22+ test files

/app/frontend/src/
  pages/ (AdminPage, Dashboard, Reports, JourneyDetail, QualityCriteria,
          ApiDocumentation, Layout, Settings(416L), Login, Journeys)
  components/ (admin/*, QualityTabV2, WebhooksTab, JourneyStartTab,
               JourneyIncidentsTab, JourneyCloseTab, HeatmapSection,
               LumiChat, EvidenceCarousel, ImageUploader, DashboardLayout,
               GuiasTab, ReviewModal, PulseBanner, PulseCell, PulseStrip)
  components/settings/ (SettingsUsersTab, SettingsClientsTab,
                        SettingsProvidersTab, SettingsSystemTab)
```

## Credentials
- dev@me.mx / LastMile2026 (Developer)
- agente@me.mx / LastMile2026 (Agent)
- yael@me.mx / LastMile2026 (Coordinator)
- proveedor@me.mx / LastMile2026 (Proveedor)

## Backlog
- [x] Validate export filters across all report sections (P0) - Done 2026-04-01
- [x] Unified Guias tab replacing Calidad + Paquetes (P0) - Done 2026-04-01
- [x] Proveedor role with read-only access (P0) - Done 2026-04-01
- [x] Liquidacion del Servicio export (Belgos/SOP format, P0) - Done 2026-04-07
- [x] Dynamic SLA Configuration per Provider (P0) - Done 2026-04-07
- [x] SLA/Pulse config edit permissions for Coordinators (P1) - Done 2026-04-07
- [x] Pulse: Real-time feasibility monitor - Done 2026-04-07
- [x] Code Quality: Security fixes, useMemo, refactoring - Done 2026-04-07
- [x] Discrepancy Detection: Confidence engine + GuiasTab UI - Done 2026-04-09
- [x] AI Evaluation UX: Severity badges, ReviewModal, Background polling - Done 2026-04-09
- [x] Bug fix: Confidence score using raw DB fields + auto-recalc after Kosmo rescrape - Done 2026-04-09
- [x] Bug fix: Kosmo batch re-scrape now fetches ALL packages (not just 0-proof) - Done 2026-04-09
- [x] Connected Quality Criteria system_prompt to AI evaluation engine - Done 2026-04-09
- [x] Supervised Training generates training_samples on manual review - Done 2026-04-09
- [x] Reports module redesign: KPI cards, charts, chip filters, PDF export - Done 2026-04-10
- [x] P1: Inline Incident Registration from Guias Tab - Done 2026-04-10
- [x] P2: AI Reports Generation v2.0 with Dynamic Cards - Done 2026-04-10
- [x] P3: Cubbo Standard Evidence Evaluation (ReviewModal) - Done 2026-04-10
- [x] Code Quality Review: Security fixes, backend refactoring, GuiasTab split, hook deps, PulseStrip key - Done 2026-04-10
- [x] PDF Export Redesign: Multi-page, legible, all 6 tabs + cover + AI analysis - Done 2026-04-10
- [x] P1: Refresh Token for Power BI (generate, exchange, revoke, list) - Done 2026-04-15
- [x] P2: Liquidacion formulas fix (Col O = H+I, Col AB = O/MAX(G,sla)) - Done 2026-04-15
- [x] P3: Delete Journey + Driver column in tables - Done 2026-04-15
- [x] P4: Order ID in tables + search + pagination 25/50/100 - Done 2026-04-15
- [x] P5: Remove checklist + 32 Mexico states in route type - Done 2026-04-15
- [x] Kosmo Sync Performance Optimization (P0) - Done 2026-04-15
- [x] httpOnly cookie migration (P1, security hardening) - Done 2026-04-15
- [x] Split Settings.jsx (1147->416L), JourneyDetail.jsx (1447->1055L) - Done 2026-04-15
- [x] P0 Driver Management: Drivers tab in Settings + Blocking modal for new providers in Layout upload - Done 2026-04-16
- [x] P1 Driver Management: Route-level provider editing + audit log (route_edits) - Done 2026-04-16
- [x] Lumi Fix: Active journey context injection (P0+P1+P2 combined) - Done 2026-04-16
- [x] Auto-Audit Report: Generated /app/memory/AUDIT_REPORT.md + Applied P0 fixes (TTL indexes, audit_log cleanup) - Done 2026-04-16
- [x] Code Quality Review: Empty catches (30+ fixed), XSS verified (DOMPurify already in place), hook deps, test credentials centralized, webpack 0 warnings - Done 2026-04-16
- [x] Platform Manuals (Knowledge Hub) Fase 1: Index + Viewer + CRUD API + 6 seed manuals - Done 2026-04-17
- [x] Sidebar colapsable (solo iconos) con persistencia en localStorage - Done 2026-04-17
- [x] P1 Split componentes >500L: Dashboard(-95L), QualityTabV2(-65L), Reports(-57L), Settings(-726L) - Done 2026-04-17
- [x] P3 Split Layout.jsx (1062->991L): WizardSteps + UpdateNotesSection extraidos - Done 2026-04-17
- [x] Vistas movil: Sidebar drawer, responsive tables, KPI cards adaptados, hamburger menu - Done 2026-04-17
- [ ] Split Layout.jsx (1062L) — wizard steps a sub-componentes (P3)
- [x] Liquidacion fixes: +Proveedor en Incidencias, Completados=delivered+failed, evidencia incluye failed - Done 2026-04-17
- [x] Update delivery notes: POST /upload/update-notes + note_from_driver in GuiasPackageDetail - Done 2026-04-17
- [x] AI Eval Worker & Monitor de Procesos: MongoDB-backed queue, background worker + cron sweep, /api/ai-evaluation/* routes, /monitor page with access control (coordinator/developer), Progress indicatorClassName support - Done 2026-04-21
- [x] Catalogo estandarizado de Tipos de incidencia (5 enum values) + campo condicional comentario_asesor (requerido con 'otro', max 500 chars) + manual actualizado - Done 2026-04-21
- [x] Code Quality P2+P3: React.lazy() en rutas no críticas (Suspense + PageLoader), refactor ai_eval_worker._process_job (95L→28L + 3 helpers), split ApiDocumentation.jsx (711→270L + DocsTab/SandboxTab/ExamplesTab), orphan job recovery al startup, clipboard API robustness, .quality-ignore.md - Done 2026-04-22
- [x] Code Quality P2+P3 batch 2: lifespan context manager (reemplaza @app.on_event), SSE json.dumps, cancel_job 404/400 clarity, GET /api/ai-evaluation/health para monitoreo externo, Dashboard.css extracted (541→505L), nuevo manual "Monitor de Procesos" + actualizado "API & Integraciones" con sección de worker monitoring - Done 2026-04-22
- [x] Code Quality P2+P3 batch 3: Split GuiasTab (654→585L + useGuiasMetrics hook + GuiasKpisRow component), Split JourneyDetail (1246→1208L + ProviderEditDialogs), Split Layout (992→931L + RoutesPreviewCard + UploadHistoryCard), Harmonizar envelope paginación via pagination_utils.paginated_response() helper con dual-shape (flat + nested) para backwards compat, Fix React duplicate key warning en PulseStrip - Done 2026-04-22
- [x] Arquitectura Viva (Living Architecture): AST-based scanner (/app/backend/architecture_scanner.py) + routes (snapshot/regenerate/history/diff/changelog) + /arquitectura page con 5 tabs y 3 diagramas Mermaid + RBAC developer/executive - Done 2026-04-22 (iter48 passed 100%)
- [x] Monitor de Procesos fixes: (a) diagnóstico MotorCollection bug histórico (37 jobs previos, bug resuelto en d4cc7ff), (b) detección explícita Budget-exceeded/RateLimit/Auth con mensajes claros en UI, (c) heartbeat last_progress_at + recuperación periódica de jobs estancados (>15 min sin progreso), (d) POST /api/ai-evaluation/jobs/retry-errors (reencola rutas con error, dedupe por route_id), (e) botón "Reintentar Errores" en UI Monitor + banner error_detail en modal, (f) cleanup de 48 jobs atascados - Done 2026-04-22
- [x] Layout "Actualizar notas de entrega" mejoras: validación cliente 10MB + extensión, drag&drop, nombre/tamaño de archivo visible, botón reset, manejo específico de errores 413/timeout/network con mensajes legibles - Done 2026-04-22
- [x] Fix bug nombres campos IA en frontend: GuiasTab/GuiasPackageDetail/ReviewModal leían `ai_score/ai_errors/ai_confidence` pero backend persiste `evidence_score/ia_errors/ia_confidence`. Agregada normalización en useGuiasMetrics con alias (65+ paquetes evaluados quedaban invisibles) - Done 2026-04-22
- [x] "Limpiar rutas y pedidos" con rango de fechas: POST /api/cleanup/routes-packages acepta date_from/date_to/dry_run + GET /cleanup/routes-packages/preview; UI con selector de alcance (rango/todo), 2 date pickers, preview live de conteos (journeys/packages/incidents/ai_jobs/route_edits/training_samples), RBAC coordinator+developer - Done 2026-04-22
- [x] Guards anti-desperdicio IA tras cleanup: (a) cleanup endpoint cancela activamente jobs En_Cola/Evaluando de las rutas eliminadas antes del delete (audit trail), (b) _process_job verifica existencia de ruta antes de cada batch y aborta marcando guías restantes como "Ruta eliminada" (0 créditos adicionales), (c) _evaluate_single_guia retorna _skip_retry para paquetes eliminados (evita 3 retries inútiles) - Done 2026-04-22
- [x] Error Tracker cleanup de ruido: middleware ahora excluye 401 globales (sesión expirada) y 404 de business-logic esperado (journey/package/client/provider/user eliminados, lumi/active-context, export-liquidacion sin datos). 79 errores históricos marcados como revisados - Done 2026-04-22
- [x] Cleanup chunked deletes: _chunked_delete helper evita timeouts MongoDB en datasets grandes (batches de 500 IDs por $in). Resuelve el 500 "read operation timed out" reportado - Done 2026-04-22
- [x] **Migración IA Evaluación: Sonnet 4.5 → Haiku 4.5** (solo evidence scoring, Lumi queda en Sonnet). Backend: nuevo módulo ai_eval_config.py con in-memory cache 30s, persistencia en config.ai_eval_config, validación con bounds; evidence_scoring.py lee modelo dinámico por job; ai_eval_worker.py lee timeout/concurrency/batch/retries por job. Endpoints GET/PUT /api/ai-evaluation/config con RBAC developer/coordinator. Frontend: nuevo tab "Motor IA" en AdminPage con selector visual de modelo (Haiku/Sonnet con info de costo, velocidad, full ID), 4 sliders (timeout 30-180s, concurrent 1-5, batch 1-10, retries 0-5), estimación de costo mensual. Validado live: Haiku 4.5 devuelve score+errors en 29s (vs 30-60s Sonnet). Ahorro esperado ~$1,476 USD/mes - Done 2026-04-22
- [x] Fix post-migración: (a) claude-haiku-4-5 agregado a `ia_cost_config` ($1/$5 por M tokens), (b) 153 entradas token_usage_log con cost_usd=0 recomputadas, (c) token_logger.FALLBACK_MODEL_PRICING para que cualquier modelo nuevo tenga precio desde el día 1 incluso si no está en config - Done 2026-04-22
- [x] **Pausa/Kill-switch del worker IA** (prevención de impacto en performance): (a) ai_eval_config extendido con `paused_until`, `pause_reason`, `schedule_enabled`, `schedule_windows[]` + función `is_worker_paused()`. (b) Worker loop verifica pausa cada tick (10s); si activa: `_kill_active_jobs_due_to_pause()` aborta jobs Evaluando marcando guías Evaluando/En_Cola como Error con "Worker pausado", finaliza jobs como Error/Parcial, 0 créditos adicionales. (c) Endpoints: POST /api/ai-evaluation/pause (1-2880 min), POST /resume, PUT /schedule con ventanas recurrentes tz CDMX. (d) UI: PauseBanner en tab "Motor IA" con presets 1h/4h/24h/personalizado + countdown live, ScheduleSection con toggle enable + editor visual de ventanas (nombre, días L-D con toggle buttons, time pickers from→to, tz CDMX, soporta overnight). (e) Banner amarillo en /monitor visible globalmente cuando worker pausado - Done 2026-04-23
- [x] Fix post-deploy UI: (a) TokenUsageTab hardcoded "claude-opus-4-5" → ahora deriva modelo del primer evento real (muestra `claude-haiku-4-5` cuando aplica), (b) auto-refresh cada 30s, (c) botón "Actualizar" manual, (d) `claude-haiku-4-5` agregado al filtro de modelos, (e) limpieza masiva de .gitignore con 5 bloques duplicados de .env que bloqueaban deploy - Done 2026-04-24
- [x] Indicador LIVE en "Detalle por evento": puntito verde pulsante + texto "LIVE · hace Ns" con tooltip "Actualización automática cada 30s" — visual confirmation de que la data se refresca sin clic manual - Done 2026-04-24
- [x] **Robustez worker IA** (reducción de desperdicio de créditos y jobs fantasma): (a) auto-pausa de 10 min al detectar Budget exceeded en batch completo (evita intentos futiles), (b) orphan recovery extendido: jobs En_Cola >2h → Error "saturación IA" (antes se quedaban indefinidamente), (c) finalización explícita con mensaje claro cuando se agota el saldo mid-evaluación - Done 2026-04-24
- [x] **Fix subestimación de costos** ($5.58 USD no contabilizados en 1 día): (a) shadow-log en timeouts y errores non-budget (evaluaciones que gastan tokens pero no quedaban en log), (b) log_token_usage agregado al generador de reportes con IA (analytics_routes — reporte_ejecutivo Sonnet shadow call), (c) actualización del doc PRECEDENT_BUDGET_INCONSISTENCY.md con cálculo real: costo/guía = $0.017 (2.36x subestimado), proyección corregida 5,530 guías/saldo actual - Done 2026-04-24
- [x] **Performance optimization** (reducción de percepción "se cayó"): (a) nuevo módulo `ttl_cache.py` — decorator `@ttl_cache(ttl_seconds, prefix, user_scope)` para endpoints read-heavy, in-memory LRU con max 500 entradas, aislamiento por role/email; (b) aplicado a 5 endpoints pesados: `/dashboard/stats` (30s), `/dashboard/incidents-breakdown` (45s), `/dashboard/provider-comparison` (60s), `/admin/summary` (60s), `/admin/token-usage` (20s); (c) kosmo_sync frecuencia configurable vía env: SCHEDULER_CHECK_SECONDS 45→90s, MAX_JOURNEYS_PER_CYCLE 25→20; (d) validación live: response times 3-33ms tras MISS inicial (vs 27s cuando backend estaba saturado con LLM calls) - Done 2026-04-24
- [x] **N+1 + 2 Uvicorn Workers safety** (2026-04-24): (a) fix 6 N+1 queries en journey_routes.py, (b) nuevo `leader_election.py` con MongoDB TTL locks — solo 1 worker arranca kosmo_sync + ai_eval_worker cuando --workers >1 (iter49 pass: '[leader] acquired bg_tasks' + '[bg] Worker is LEADER'), (c) heartbeat loop refresca lease cada 10s, grace window ≤30s al fallar leader
- [x] **P0 Fix ImportError /api/ai-evaluation/health** (2026-04-24): el router importaba `MAX_ROUTES_CONCURRENT` de ai_eval_worker (eliminada al migrar a config dinámica). Corregido para leer cfg['max_routes_concurrent'] via get_ai_eval_config(db). iter49: 200 OK con payload completo {status, worker{max_concurrent, slots_in_use, slots_free, cron_interval_minutes}, queue, last_terminal_job}
- [x] **Split Layout.jsx** (2026-04-24, P3): 944L→447L (-53%). Extraídos 5 componentes: LayoutStep1History, LayoutStep2Route, LayoutStep3Config, LayoutCreationResult, LayoutDialogs (Confirm+PendingProvider). Sin cambios de UX, comportamiento idéntico, iter49 pass
- [x] **PDF Export Journey Details** (2026-04-24, P2): nuevo lib/journeyPdfExport.js con jsPDF+autoTable. 6 secciones (cover+resumen, KPIs, inicio, tabla de paquetes [hasta 400], incidencias, cierre). Botón 'Exportar PDF' en header de JourneyDetail.jsx (data-testid=export-journey-pdf-btn). Footer con paginado, cap en 400 pkgs con hint hacia export Excel
- [x] **Compresión automática de imágenes** (2026-04-24, P2): nuevo lib/imageCompress.js — Canvas API, umbral 1MB, redimensiona a max 1920px longest side, JPEG 85% (PNG conserva transparencia). Integrado en ImageUploader.jsx vía compressImages() antes del upload. Toast 'X imagen(es) optimizada(s) · Y MB ahorrados'. Sin nuevas dependencias
- [x] **Tendencias históricas en Reports** (2026-04-24, P3): nueva tab 'Tendencias' (TrendsTab.jsx) usando /api/reports/kpis existente. 4 KPIs resumen + delta vs primera semana, LineChart de delivery_rate, ComposedChart stacked (entregados/fallidos/pendientes) + línea de rate %. Toggle Día/Semana/Mes (data-testid=trends-groupby-*). iter49: 10 SVGs recharts renderizando con 33 journeys / 1399 pkgs
- [x] **PDF reporte operativo: 13 correcciones solicitadas por usuario** (2026-04-24, P0). Rehecho /app/frontend/src/lib/pdfReportGenerator.js: (1) cards portada con altura 46mm + splitTextToSize → no se cortan labels; (2) 'Tasa de entrega' compara vs target SLA dinámico ('vs target 75%' o 90%); (3) 5 stat boxes visibles (Total/Entregados/Fallidos/Pendientes/Rutas) con tira de color; (4) 'Rutas' lee total_journeys (antes total_routes inexistente → siempre 0); (5) 'Pendientes' = total - entregados - fallidos con leyenda explicativa; (6) donut nativo jsPDF portada (3 universos + leyenda + center total); (7) donut nativo incidencias (reemplaza captura html2canvas que se cortaba) + tabla con '% del total'; (8-9) eliminada columna 'Dias op.' de tablas Proveedores y Drivers (reporte diario); (10) 'Analisis IA' → 'Herramientas tecnologicas'; (11) AI cards solo en sección Herramientas (removidas del cover) con altura 32mm + 4 líneas de body; (12) paginación narrativa IA corregida (bottomLimit + addPage); (13) cleanText() strip caracteres no Latin-1 (emojis/iconos ilegibles). Helper drawDonut() nativo con pdf.triangle() + círculo interior blanco. iter50: PDF 148KB, 7 páginas, 11/13 fixes validated end-to-end con pdfplumber, 2 fixes verified por code review


- [ ] Mobile-optimized views for field agents (P2)
- [ ] Fix React console warnings: indicatorClassName prop, duplicate keys (P3)
- [ ] 2FA TOTP para roles críticos (P2, ver /app/memory/proposals/PROPUESTA_2FA.md)
- [ ] P01 Storage Risk: ver /app/memory/P01_STORAGE_RISK.md (P2)

- [x] **iter60 — Auditoría exhaustiva Evaluación IA + 5 fixes preventivos** (2026-04-26, P0). Sesión post-redeploy de iter59 detectó toasts transitorios "Error al cargar eventos de tokens" y "Error al cargar resumen admin" durante cold-start del pod. Auditoría profunda del sistema completo Evaluación IA reveló 3 hallazgos críticos: (a) **CAUSA RAÍZ DE ZOMBIS RECURRENTES — `litellm.completion()` sin timeout**: la SDK `LlmChat.send_message()` llamaba a `litellm.completion(**params)` sin parámetro timeout. Si Anthropic se colgaba (red/rate-limit/overload), el `await` quedaba esperando ∞ → worker hangueado → slot bloqueado permanentemente → cron de recovery solo lo detectaba después de 30 min. Fix doble: (1) `chat.with_params(timeout=cfg.get('llm_timeout_seconds', 90))` honrado por la SDK, (2) `asyncio.wait_for(send_message, timeout=hard_timeout)` como outer guard defensivo (100s). Si timeout dispara → log shadow cost con `shadow_kind='hard_timeout_exceeded'` + raise → job va a Error → slot libre. (b) **Recovery cron muy lento**: bajado de 30 ticks (5min) → 18 ticks (3min) en `ai_eval_worker.py`. Combinado con threshold 10 min de stuck = recovery total ~13 min vs 20 min antes. (c) **Cold-start UX**: `AdminPage.fetchSummary` y `TokenUsageTab.fetchEvents` ahora hacen retry silencioso 1× con backoff 1.5s en errores transitorios (status >= 500 o 0). Toast solo después del 2do fallo. Eliminado bug donde primer paint del /admin post-deploy mostraba toasts rojos mientras el pod aún arrancaba. **Validación**: 33/33 unit tests verdes, lint clean, asyncio.wait_for guard probado, backward-compatible (`cfg.get` con default 90s). **Resultado esperado**: durante 24h post-deploy, ningún slot debe tener Evaluando >120s con tokens=0. El bug recurrente de zombis está definitivamente cerrado.

 (2026-04-26, P0). Sesión de bugfixes pre-deploy: (a) **Routal API client v2**: corregidos 3 paths que usaban prefijo viejo (`/plans` → `/v2/plans`, `?page_size` → `?limit`, `project_id` ahora opcional) — validado E2E con la api_key real del usuario, devuelve `total: 5505` plans contra `api.routal.com`. (b) **Routal webhook receiver**: agregados handler GET (healthcheck 200), OPTIONS (CORS preflight 204), body vacío como test ping, multi-header signature (`X-Routal-Signature`/`X-Webhook-Signature`/`X-Hub-Signature-256`), 404 con mensaje descriptivo de qué hacer si la integración está inactiva. (c) **RBAC consistency** en 8 endpoints: `POST /clients`, `POST /providers` ahora `[coord, dev]` (developer estaba excluido); `POST /journeys`, `POST /incidents`, `PUT/DELETE /incidents`, `POST /upload-history`, `POST /resolve-all-incidents` ahora `[coord, agent, dev]`. Eliminada incoherencia donde agent podía crear catálogos pero developer no. (d) **Recuperar rango de fechas SEL01**: nuevo endpoint `POST /api/selection/run-range/{client_id}?date_from=&date_to=` (idempotente, max 90 días) + UI con date pickers + detalle por día expandible. (e) **AI worker zombi recovery**: thresholds bajados (15→10 min stuck, 30→15 min orphan); nuevo endpoint manual `POST /api/ai-evaluation/recover-stuck` (developer/coordinator) para destrabar slots cuando el cron no actúa. (f) **Telemetría Kosmo**: corregido bug donde `/api/health` y `/test` Kosmo leían el campo inexistente `last_kosmo_sync_at` en vez del real `last_sync_at` (worker SÍ funcionaba, solo el reporte estaba mal). (g) **PII defense-in-depth**: `_visibility()` ahora fail-secure (roles desconocidos → masked); agregado `recipient_phone` a `PackageBase` schema; nuevo unit test. (h) **`.gitignore` cleanup**: eliminados 3 bloques duplicados que ignoraban `.env` files (bloqueaba deployment). 33/33 unit tests verdes (iter55+56+58).


- [x] **iter70 — Consistencia ID Routal en Dashboard** (2026-04-28, P2). Usuario reportó que `/journeys` muestra el plan ID Routal pero el Dashboard `/` (tabla de Rutas del día) no. (a) `RoutesTable.jsx` (componente del Dashboard): columna "Order ID" → "Origen / ID" con misma lógica que `/rutas` (iter66): badge verde **"ROUTAL"** + label dinámico (`routal_plan_label` o id truncado) + icono ExternalLink al Routal Planner si existe `routal_project_id`. Para journeys con `order_id` (Kosmo): muestra el order_id en azul como antes. Para legacy/sin ambos: "-". (b) Validado E2E preview: 10 badges Routal visibles + links externos clickables al cambiar filtro de fecha al 24-26 abr donde hay journeys reales. Tres mejoras de UX/recursos según roadmap `RT_LastMile_OS_Sprints.md`. (a) **RT-20 (Sprint 3)**: Vista de paradas en GuiasTab ahora muestra teléfono y domicilio del destinatario debajo del nombre. Teléfono es clickable (`tel:`) con icono 📞, dirección con icono 📍 truncada con tooltip completo. PII sigue cifrada en DB y desencriptada al servir según rol. Validado E2E preview: 33/33 direcciones visibles (Routal trae sin teléfono, Kosmo trae ambos). (b) **RT-12 (Sprint 2)**: Botón verde **"Re-ejecutar SEL01"** en `/rutas` visible para roles `coordinator` y `developer`. Itera todos los clientes con `selection_enabled=true && active=true` y corre `runSelection(client_id)` con confirmación. Toast resumen con drivers seleccionados y errores. Idempotente (drivers ya seleccionados se preservan). (c) **RT-04 (Sprint 1)**: Tabs "Inicio" y "Fin" siguen visibles para journeys Routal (sólo se actualizó copy en JourneyCloseTab: "Pre-seleccionados desde la fuente (Kosmo / Routal)"). Backend `/journeys/{id}/start` y `/close` no validan source — funcionan independientes. **NO eran feature gaps, sólo confusión de copy.** Validados E2E.

- [x] **iter71-iter73 — Sprint 1+2 multi-proyecto Routal: backend + UI + test connection** (2026-04-28, P1). Sprint 1 (RT-13 sucursales + RT-01 multi-proyecto Routal + RT-02 toggle rollback) + Sprint 2 UI completa. **Backend**: (a) Nueva colección `client_branches` con CRUD vía `/api/branches` (RBAC dev/coord). Migration helper `POST /api/branches/migrate-cubbo-cities` crea Cubbo CDMX/GDL/MOR/MTY/PUE/QRO idempotente. Schema journey/package extendido con `branch_id` opcional (no breaking). (b) `client_integrations.branches[branch_id] = {credentials_encrypted, active, updated_at}` con API key + project_id + webhook_secret cifrados Fernet POR sucursal (no compartidos). (c) `IntegrationService.get_routal_client(client_id, branch_id)` factory multi-tenant con fallback a single-project legacy. (d) `set_branch_credentials`, `find_client_branch_by_routal_signature`, `list_active_routal_branches`. (e) Webhook handler detecta automáticamente `branch_id` del `payload.project_id`, valida HMAC con secret específico de la sucursal, y propaga `branch_id` al evento + journeys + packages + routal_daily_plans. (f) Endpoints: `PUT /api/branches/{id}/routal-credentials`, `GET /api/branches/{id}/routal-credentials` (sin valores raw), `PATCH /api/branches/{id}/routal-toggle?active=` para RT-02 rollback Kosmo. (g) Índice único `(client_id, code)` + `branch_id` en journeys. **Frontend**: nuevo tab **"Sucursales"** en Settings con card por cliente: lista 6 sucursales con badges ROUTAL OK/SIN ROUTAL/Inactiva, journeys count, ID truncado. Form expandible para credenciales con API key show/hide, Project ID, Webhook Secret. Botón "Crear 6 sucursales estándar" para Cubbo (visible si vacío). Toggle activo + delete soft. Validado E2E en preview: 6/6 sucursales Cubbo creadas, credenciales CDMX guardadas y cifradas (project_id `636aaed5...`), badge "ROUTAL OK" actualizado, toggle on/off funciona.

- [x] **iter74 — RT-09 SEL01 agregado nivel cliente + iter75 RT-11 scheduler_times[] + probe sin guardar** (2026-04-28, P1). Sprint 1+2 cerrado completamente. (a) **RT-09**: algoritmo SEL01 (`run_daily_selection`) ahora suma drivers de TODAS las sucursales antes de aplicar `max_daily_audits` (no por sucursal). El query `routal_daily_plans` ya filtra por `client_id`, así que la agregación es natural — sólo agregué `branch_id` propagado al `_handle_plan_created_direct` y al `driver_audit_log`. Response del run incluye `branch_breakdown: {CDMX: {total, selected}, GDL: {...}, ...}` con códigos de sucursal resueltos para telemetría. Validado: 4 plans tageados aleatoriamente entre QRO/GDL/MOR → algoritmo seleccionó 4/4 de los 3 branches con desglose correcto. (b) **RT-11**: `client_config.scheduler_times: List[str]` (1-10 entries) coexiste con `scheduler_time` legacy (back-compat). Scheduler loop ahora itera todos los cutoffs configurados y trackea `last_scheduled_run_dates: {time: yyyy-mm-dd}` independiente para cada uno (e.g. corte 06:00 puede haber corrido sin que 12:00 lo haya hecho aún). UI: chips removibles `06:00 ×` `12:00 ×` `18:00 ×` con input HH:MM nativo + botón `+`. Validado: payload `{"scheduler_times":["06:00","12:00","18:00"]}` aceptado y persistido + `scheduler_time` (legacy single) actualizado a `06:00` automáticamente. (c) **Probe sin guardar**: nuevo endpoint `POST /api/branches/probe-routal-connection` recibe `{api_key, project_id}` ad-hoc y los prueba sin persistir. Botón "Probar conexión" ahora muestra "Probar (sin guardar)" cuando hay valores en el form, y "Probar conexión" cuando solo se valida lo guardado. Validado: con creds reales devolvió 5 plans visibles (LastmileScanSessions reales), con creds inválidas devolvió HTTP 401 + hint accionable.

- [x] **iter68 — Fix Evaluación IA sobre imágenes Routal** (2026-04-26, P0). Usuario reportó: Evaluación IA manual sobre guía Routal `B4OlPyL7VEgSr7Z6` no interpretó la imagen como hace con Kosmo. Causa raíz: `_download_image_as_base64` en `evidence_scoring.py` usaba `httpx.get(url)` para todas las URLs, pero las URLs Routal son **relativas** (`/api/integrations/routal/image/{client_id}/{report_id}/{image_id}`) — httpx fallaba sin URL absoluta y la IA recibía 0 imágenes, retornando feedback vacío + score bajo de fallback rules. Las URLs Kosmo son absolutas (CDN/S3) por eso sí funcionaban. (a) Detección de URLs proxy interno por path prefix → bypass HTTP+auth, llamada directa a `proxy_routal_image(db, client_id, report_id, image_id)`. Aprovecha el cache disco backend (165ms vs 870ms) y evita round-trip de auth. (b) Param `db` opcional propagado a `evaluate_single_package_ai` y a sus dos callsites (`_eval_one` worker batch + endpoint puntual `evaluate_package_evidence`). (c) Validado E2E en preview con package Routal real: descarga 1473719 bytes (1.4MB) base64-encoded en <200ms, IA Claude analizó la imagen correctamente: detectó "Puerta/portón metálico blanco con número '20'", quality 'buena', criteria_met con foto_fachada=true, nota_driver=true, missing items detallados, score 25/100 (correctamente bajo porque Routal trae solo 1 foto vs estándar Cubbo de 3-5), confidence 0.95, feedback completo en español, 3703 tokens. (d) **Importante: el score bajo NO es un bug** — refleja que las entregas Routal típicas tienen menos evidencia visual que las Cubbo (1 foto + firma + nota vs 3-5 fotos + 2 llamadas + receptor). Si Cubbo quiere re-calibrar el estándar para entregas Routal específicamente, se puede ajustar el system prompt para esa fuente.

- [x] **iter67 — Performance audit + fixes (P0 quick wins)** (2026-04-26, P0). Usuario reportó lentitud en PROD. Profiling identificó tres hotspots medibles. (a) **Image proxy SIN cache** (impact: 870ms por imagen, 1.4MB descarga Routal cada hit). Un journey con 33 paquetes × 2 fotos = 66 hits × 1s = ~60s en abrir el detalle, ~60MB tráfico. **Fix**: cache on-disk en `/tmp/routal_image_cache/` con TTL 7 días (URLs Routal son inmutables por `report_id+image_id`), coalescing de fetches concurrentes vía `asyncio.Lock` por key. Resultado validado: 870ms → **165ms** (**5.3× más rápido**) para hits cacheados. Configurable via env `ROUTAL_IMAGE_CACHE_DIR` y `ROUTAL_IMAGE_CACHE_TTL_SECS`. (b) **SecurityHeadersMiddleware aplicaba `Cache-Control: no-store` ciegamente a TODO `/api/*`** incluso para imágenes inmutables. **Fix**: allowlist en middleware para `/api/integrations/routal/image/` y `/signature/` que sirve `Cache-Control: private, max-age=2592000, immutable` + ETag. (En preview Cloudflare sobreescribe con `no-store` — en PROD `lastmile-mvp.emergent.host` puede o no preservarlo según config Cloudflare; el cache backend funciona en cualquier caso.) (c) **Worker periódico Routal sync (10min)** ejecutaba sync sobre TODOS los journeys activos sin filtros, incluyendo los ya 100% resueltos. **Fix**: skip de journeys con `routal_synced_at < 5min` (env `ROUTAL_SYNC_MIN_RESYNC_MIN`) y skip de journeys donde `delivered+failed >= total` (ya estables). Reduce carga del worker ~80% en estado estable, libera el event loop para requests usuario. (d) Reporte de latencias preview post-fix: `/api/journeys?limit=50` 100-170ms · detail+packages 120-140ms · image hot 165-220ms. Todos saludables.

- [x] **iter66 — Columna Origen + link directo al Routal Planner en /rutas** (2026-04-26, P2). Para reconciliación cruzada rápida entre LastMile y Routal sin tener que entrar al detalle de cada journey. (a) Backend persiste `routal_project_id` en journey docs (extraído de `payload.project_id` al crear, hidratado en sync desde `detail.project_id`/`organization_id`). (b) Frontend `/rutas`: columna "Order ID" reemplazada por "Origen / ID" enriquecida — si `journey.source==='routal'` y hay `routal_plan_id`: muestra badge verde **"ROUTAL"** + label dinámico (`routal_plan_label` o id truncado) + icono ExternalLink que abre `https://planner.routal.com/h/{project_id}/planner/plan/{plan_id}/stops` en nueva pestaña (rel=noopener). Si hay `order_id`: muestra como antes (Kosmo). Si no: '—'. (c) Buscador extendido: acepta también `routal_plan_id` y `routal_plan_label`. Validado en preview: 10 badges Routal + 9 links externos clickables, deliveries sincronizadas vía worker (50/56=89%, 42/44=95%).

- [x] **iter65 — Routal pipeline transparente como Kosmo (worker periódico + recipient_name + plan ID UI)** (2026-04-26, P0). Tres fixes solicitados por usuario para que la experiencia Routal sea idéntica a Kosmo. (a) **Worker periódico `routal_sync_worker.py`**: loop async gated por leader_election, cada 10 min (env `ROUTAL_SYNC_INTERVAL_MINUTES`) busca journeys con `source='routal'` y status in `['planificada','en_ruta']` (lookback 7 días), corre `sync_journey_from_routal` en paralelo (semaphore=3). Validado en preview: primer tick procesó 9 journeys de Cubbo, sincronizó 110 paquetes delivered + 8 failed + 335 recipient_name rellenados automáticamente. (b) **Mapeo recipient/address**: nueva función `map_routal_stop_to_pkg_fields` extrae de cada `stop` Routal: `recipient_name=stop.label`, `address=stop.location.label` (fallback construyendo desde street/house_number/postal_code/city/state), `recipient_phone=stop.phone`, `tracking_number=external_id|fixed_id|id`, `order_reference_id=client_external_id|fixed_id|id`. Usado en `_handle_plan_created_direct` (creación nuevos webhooks) y `routal_sync._build_recipient_update` (reconciliación journeys ya existentes; PII cifrado al persistir). El sync llena destinatario incluso para stops `pending`. (c) **UI Plan ID**: header de JourneyDetail ahora muestra badge verde **"ROUTAL"** + label dinámico (`routal_plan_label` ej `"2026/04/24 - Plan"`) o `routal_plan_id` truncado, con click-to-copy y tooltip. Solo visible si `journey.source === 'routal'`. (d) Validación E2E: tabla Guías muestra destinatarios reales (Laura Luz Abad Contreras, Martha Cortés Zamora, Luis Adrián Perez), addresses formateadas (`Av. Arneses 20, San Antonio Culhuacan, Iztapalapa, 09800 Ciudad de México, CDMX, Mexico`), order_reference_id 1/2/3, todo con PII cifrada en DB y desencriptada al servir. Flujo idéntico al Kosmo, transparente para usuarios.

- [x] **iter64 — Routal Journey Sync (status + evidence) con image proxy** (2026-04-26, P0). Usuario reportó: parada `69ee361e15e80d241f91462b` completada en Routal con fotos+firma del 26/04 11:02, pero LastMile la mostraba "Pendiente" sin evidencia. Causa: `_handle_stop_completed` solo actualizaba status y `delivered_at`, no extraía fotos/firma/comments del `report` de Routal. (a) Nuevo módulo `services/routal_sync.py` con `sync_journey_from_routal(db, journey_id, api_base)`: consulta `GET /v2/plan/{id}` (que trae stops + reports anidados), itera packages por `routal_service_id`, copia status/delivered_at/proof_urls/driver_note/signature/report_id desde el `report` (type='service_report_completed'). Maneja también `failed/incomplete/canceled` con `fail_reason` desde comments. Recalcula counts del journey vía aggregate. Idempotente (skip si ya delivered con mismo proof_count). (b) Image proxy `proxy_routal_image(db, client_id, report_id, image_id)`: descarga bytes desde `https://api.routal.com/v3/stop/report/.../image/...` autenticando con la API key cifrada del cliente (Fernet) — la key NUNCA expuesta al browser. Cache 1h via header. (c) Endpoints: `POST /api/integrations/routal/sync-journey/{journey_id}` (RBAC dev/coord/agent), `GET /api/integrations/routal/image/{client_id}/{report_id}/{image_id}?kind=image|signature`. (d) URLs almacenadas como `kosmo_proof_urls` (relativas `/api/...` que el ingress mapea a backend) → frontend ya las renderiza con `<img src=>` sin cambios. Campo nuevo `routal_signature_url` para la firma. (e) Frontend: botón **"Re-sincronizar Routal"** (RefreshCw, outline) condicional a `journey.source === 'routal'` reemplazando "Re-sincronizar Kosmo" en el header de GuiasTab. Toast con counts: entregadas/fallidas/pendientes/sin-match. (f) Validación E2E en preview con journey de Juan Cervantes (33 paquetes, plan Routal `69eb8f25ab92988fd9a5abee`): sync devolvió `4 delivered + 1 failed + 28 pending`, journey.packages_delivered actualizado de 0→4, image proxy descarga PNG real de 1.4MB, foto de evidencia (puerta con número 20) renderizada en carrusel, driver note "Recibe titular" guardada. End-to-end completo.

- [x] **iter63 — Fix discrepancia journey.date vs Routal execution_date (1 día offset) + UI Reconciliar fechas** (2026-04-26, P0). Usuario reportó: journey LastMile id `4848685a-b95f-4255-9e35-226969034857` mostraba "25 abr 2026" pero Routal Planner del mismo plan (`69ee28ea0b5c1d1cc8ba6704`) decía "26/04/2026". Validación contra Routal API: `execution_date=2026-04-26T18:00:00Z`, label `"2026/04/26 - Plan"` — Routal autoritativo (UTC=CDMX día 26). Causa raíz: `_handle_plan_created_direct` y `_handle_plan_created` usaban `payload.get("date")` como fuente primaria. Routal envía ese campo en formatos heterogéneos. El campo estable es `execution_date`. (a) Nueva función `_extract_plan_date(payload)` con prioridad: 1) `execution_date` (ISO, autoritativo), 2) `date` (string corto o ISO), 3) UTC today. (b) Aplicado en ambos paths: webhook directo y SEL01 staging. (c) Nuevo endpoint `POST /api/selection/reconcile-dates/{client_id}?days_back=30&dry_run=true` que compara `journey.date` vs Routal `execution_date` y corrige discrepancias en journeys ya creados (sin re-procesar webhooks). (d) 9/9 unit tests verdes en `test_iter63_plan_date.py`. Total: 35/35 unit tests verdes (iter56+iter58+iter63). (f) **UI Reconciliar fechas en Settings → Auditorías**: nuevo botón violeta "Reconciliar fechas" con panel expandible, input "Días hacia atrás" (1-365), botón "Detectar discrepancias" (dry-run) y "Aplicar fix" (deshabilitado hasta haber discrepancias, con `window.confirm`). Tabla con columnas Driver / Plan Routal (truncado) / LastMile (rojo) / → Routal (verde). Test E2E en preview: Cubbo → 9/9 journeys sin discrepancias (esperado, hechos vía backfill).

- [x] **iter62 — Backfill SEL01 desde Routal API (sin esperar webhooks)** (2026-04-26, P1). Nuevo endpoint `POST /api/selection/backfill-from-routal/{client_id}?date_from=&date_to=&auto_run_selection=true` (RBAC developer-only). (a) Pagina `GET /v2/plans?limit=100&offset=N` (max 50 páginas / 5000 plans escaneados), filtra por `execution_date` dentro del rango, hidrata cada plan con `GET /v2/plan/{id}` (trae stops + routes + driver), construye payload tipo webhook y hace upsert idempotente en `routal_daily_plans` por `(client_id, driver_id, date)`. (b) Por defecto invoca `run_daily_selection` día por día tras hidratar. Cap de seguridad 200 planes por llamada. (c) Ajuste en `IntegrationService.get_routal_client`: ya no requiere `project_id` (solo `routal_api_key`). (d) Frontend: nuevo botón **"Backfill Routal"** junto a **"Procesar staged"** en panel "Recuperar rutas en rango histórico". (e) Validación E2E en preview con Cubbo: rango 2 días → 200 plans escaneados → 10 in-range → 13 staged → 13/13 drivers seleccionados → 9 journeys reales creados. Re-corrida idempotente. Tiempo: 2s.

- [x] **iter78 — Sparkline tendencia 14 días por sucursal** (2026-04-28, P3). Cierre del nice-to-have del item RT-09. (a) Backend: nuevo endpoint `GET /api/selection/branch-history/{client_id}?days=14` (default 14, max 90, RBAC dev/coord/exec) que agrega `driver_audit_log` por (date, branch_id) con `$group` de Mongo y resuelve códigos de sucursal con un solo lookup adicional a `branches`. Devuelve `branches:[{code, branch_id, name, points:[{date, total, selected}], total_sum, selected_sum}]` con todos los días materializados (incluso ceros). (b) Frontend: nuevo componente inline `BranchSparkline` (SVG puro 88×22px sin dependencias) — una barra por día, altura proporcional al ratio selected/total, color verde si ≥50%, azul si >0, gris si total=0. Tooltip con resumen `selected/total (overall%)`. Renderizado en sección "Tendencia · últimos 14 días" debajo del desglose, una fila por sucursal con label `code` + barras + total agregado. (c) Validación E2E: Cubbo en 2026-04-25 muestra GDL/MOR/QRO con barra verde puntual el día 25, totales 1/1, 2/2, 1/1.

- [x] **iter77 — UI Desglose por sucursal en Resumen de hoy + date picker histórico** (2026-04-28, P2). Cierre del item RT-09 lado UI. (a) Backend: `GET /api/selection/summary/{client_id}` ahora agrega `branch_breakdown: {CDMX: {total, selected, branch_id, name}, GDL: {...}}` computado desde `driver_audit_log` filtrado por fecha. Lookup adicional a `db.branches` para resolver código humano-legible. (b) Frontend `SettingsAuditTab.jsx`: nueva sección "Desglose por sucursal" debajo de los 4 KPIs del panel Resumen, mapea `Object.entries(branch_breakdown)` y renderiza chips coloreados (verde ≥50%, azul >0, gris 0%) con formato `QRO · 1/1`. (c) **Date picker** integrado al header del Resumen: input `<input type="date">` permite navegar a fechas históricas sin abrir endpoints manualmente; switching de fecha llama `getSelectionSummary` con loader. (d) Validación E2E preview: Cubbo en 2026-04-25 → 3 chips QRO 1/1, GDL 1/1, MOR 2/2 renderizados correctamente; phase_applied=mixed (P1:3, P2:1). Today (sin datos) oculta sección automáticamente.

- [x] **iter61 — Validación E2E del fix `client_config no existe`** (2026-04-26, P0). Cierre del último issue del fork anterior. (a) Backend smoke tests con curl validados live: (1) `POST /api/selection/run-range/{fake-client-id}` → HTTP 400 "Este cliente no tiene configuración SEL01. Ve a Settings → Auditorías…" (antes era 404 feo); (2) Grupo Nadro (selection_enabled=false) → HTTP 400 "El cliente tiene selección desactivada (auto-crear legacy). Activa 'Selección habilitada' primero"; (3) Cubbo (selection_enabled=true) en rango 2026-04-19 → 04-26 → HTTP 200 con `total: 0, days_processed: 8, days_with_data: 0` (ok=true en cada día). (b) Confirmado en MongoDB: `routal_daily_plans` para Cubbo tiene **0 docs** — explica los "0 rutas" reportados por el usuario en CDMX (es comportamiento correcto, no bug; la colección solo se llena cuando llega webhook `plan.created` de Routal). (c) Frontend smoke screenshot del tab "Auditorías" → UI carga correctamente, Cubbo SELECCIÓN ON, panel "Recuperar rutas en rango histórico" se expande con date pickers Desde/Hasta y botón "Procesar rango" funcional. Issue cerrado definitivamente.

- [x] **Kosmo & Manual integration handlers funcionales** (2026-04-25, P2). El endpoint `POST /api/integrations/{client_id}/test` ya no devuelve stub para tipos no-Routal: (a) **Kosmo**: health real con `scraper_status` (ok ≤2h, stale ≤12h, down >12h, never_synced), `last_sync_at`, `last_sync_age_hours`, `journeys_active_today`, `journeys_pending_sync` — sin requerir API key. (b) **Manual**: estadísticas con `manual_status` (ok/dormant/empty), `total_journeys`, `recent_journeys_30d`, `latest_journey_date` — para clientes que cargan vía Layouts. (c) Frontend `ClientIntegrationCard.jsx` ahora habilita el botón "Probar conexión" para los 3 tipos; el `test-result` muestra UI específica por tipo (Kosmo: "scraper {status}" + sync_age, Manual: "estado {status}" + counts, Routal: latency). Toast diferenciado por tipo. Shape consistency: todos los retornos incluyen `type` field. iter57: 19/19 backend tests pass + iter55 (28/28) + iter56 (9/9) regressions verdes.
- [x] **Documentación y Manuales — 14 manuales actualizados** (2026-04-25, P2). (a) Catálogo refactorizado a `/app/backend/data/manual_seed.py` con `get_manuals_seed()` retornando 14 manuales canónicos. (b) Endpoint `POST /api/manuals-admin/seed?force=true` ahora hace **upsert por slug** (antes hacía skip si existían) — `force=false`: solo inserta nuevos; `force=true`: sobreescribe content y bumpea version. (c) Nueva sección "Inteligencia Artificial" + "Documentación Técnica". (d) **6 manuales nuevos**: Integraciones SaaS multi-fuente (Routal/Kosmo/Manual con HMAC y Fernet), Auditorías SEL01 (algoritmo 2-fase + scheduler configurable), Webhooks Plug & Play, Admin IA / Motor IA (Pause/Resume/ventanas), Lumi Asistente Conversacional, Quality Criteria V2. (e) **1 manual técnico nuevo**: Diccionario técnico (modelo de datos completo: journeys, packages, client_integrations, client_config, routal_daily_plans, driver_audit_log + cálculos km_traveled / delivery_rate / packages_to_retry + identidad de cierre + endpoint Power BI con DAX measures). (f) **Actualizados** Dashboard Principal (Pulse banner, ErrorBoundary, useApi), Reportes (7 tabs incluyendo Tendencias, donut nativo, narrativa IA), Detalle de Ruta (close_data, compresión imagen 1MB, Smart Autopause), Monitor (banner pausa, kill-switch), Config (8 tabs incluyendo Integraciones+Auditorías). Total live en /manuales: 16 (14 catálogo + 2 legacy).

- [x] **Routal SaaS multi-fuente integration completa** (2026-04-25, P0). (a) Backend: nuevos archivos `utils/encryption.py` (Fernet con auto-gen key persistida en `config.encryption_key`), `services/integration_service.py`, `services/routal_client.py`, `routes/integration_routes.py` (GET/POST/PATCH/DELETE /api/integrations/{client_id}), `routes/routal_webhook_routes.py` (POST /api/webhooks/routal/{client_id} con HMAC-SHA256 + GET .../status), `workers/routal_event_processor.py`. (b) RBAC: guard `_require_developer` retorna 403 a agent/coordinator en todos los endpoints. (c) Cifrado: API keys, webhook secrets nunca expuestos en respuestas (`credentials_summary` solo devuelve flags has_api_key/has_webhook_secret + project_id público). HMAC verification con `hmac.compare_digest` constant-time, tolera prefijo 'sha256='. Idempotency por event_id. Soft-delete limpia credenciales y deja status=inactive. (d) Frontend: nuevos componentes `SettingsIntegrationsTab.jsx` y `ClientIntegrationCard.jsx`, nueva pestaña "Integraciones" en `Settings.jsx` solo visible si `hasRole(['developer'])`. (e) Selector tipo (Manual/Kosmo/Routal), credenciales Routal con eye-toggle, switches config (auto_create_journeys, auto_close_journeys, sync_drivers), botones Guardar/Probar conexión/Activar/Eliminar, panel de status webhook con counts y last_event_at. (f) `cryptography` agregado a requirements.txt. iter55: 7/7 unit + 12/12 backend E2E + 100% frontend E2E pass. Cubbo y Grupo Nadro como clientes de prueba
- [x] **Routal Selection Module SEL01** (2026-04-25, P0). Algoritmo de selección 2-fase para auditoría diaria de drivers (Cubbo: 30/día). (a) Backend nuevo: `services/client_config_service.py` (CRUD + validación + bootstrap automático Cubbo por nombre), `workers/routal_selection_worker.py` (`_select_drivers` función pura determinista por seed YYYYMMDD + `run_daily_selection` orquestador idempotente + `start/stop_selection_scheduler` loop async polling 60s gated por leader_election), `routes/selection_routes.py` (5 endpoints: POST /selection/run, GET /selection/summary, GET /drivers/audit-history, GET/PATCH /client-config/{id}, GET /client-config). (b) `workers/routal_event_processor.py` refactor: `_handle_plan_created` ahora consulta `client_config.selection_enabled`; si true → stage en `routal_daily_plans` (idempotente por client_id+driver_id+date), si false/missing → llama `_handle_plan_created_direct` (lógica original intacta = NO breaking). (c) 3 colecciones nuevas: `client_config` (max_daily_audits, selection_enabled, scheduler_time, active, last_scheduled_run_date), `routal_daily_plans` (staging buffer), `driver_audit_log` (source-of-truth selected/unselected con phase y audit_count_30d_at_selection). Índices únicos compuestos para evitar dup-staging y dup-decisión por día. (d) Frontend: nueva pestaña "Auditorías" en Settings (solo `hasRole(['developer'])`), `SettingsAuditTab.jsx` con tarjeta por cliente: switch selection_enabled, input maxDaily (1-500), input time HH:MM (scheduler CDMX configurable, no hardcoded), badge SELECCIÓN ON/Auto-crear (legacy), Resumen de hoy (planes/seleccionados/no-selec/P1+P2), botón Ejecutar ahora, detalle expandible de drivers seleccionados. (e) Algoritmo: Phase 1 puro (≥max_daily fresh→random sample), Phase 1 partial + Phase 2 (toma todos los fresh + completa con menor audit_count_30d), Phase 2 puro (todos auditados ayer→rotación por count). Tie-break determinista shuffle+sort estable. (f) Bootstrap: Cubbo se busca por `clients.name='Cubbo'` y se crea con max=30, enabled=true, time='06:00'. Otros clientes auto-create disabled-legacy en primer GET. iter56: 9/9 unit + 17/17 E2E + 100% frontend pass. Sin bugs, sin action items.
