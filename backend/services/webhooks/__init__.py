"""Webhooks salientes (PROMPT 39 V3) — punto único de salida hacia URLs externas.

Reglas R43 (HMAC saliente), R44 (suscripción explícita), R45 (anti-SSRF),
R46 (idempotencia at-least-once), R47 (delivery_log append-only), R48
(dispatcher único), R49 (PII bajo consent).
"""
